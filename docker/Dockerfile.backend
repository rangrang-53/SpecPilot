# Backend Dockerfile
FROM python:3.11-slim

WORKDIR /app

# 의존성 설치
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 애플리케이션 코드 복사
COPY backend ./backend
COPY config ./config
COPY .env.example .env

# 환경 변수 설정
ENV MODEL_NAME=gemini-3-pro

# 포트 노출
EXPOSE 8000

# FastAPI 실행
CMD ["uvicorn", "backend.presentation.main:app", "--host", "0.0.0.0", "--port", "8000"]
